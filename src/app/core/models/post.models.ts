export class PostData {
  // id: number;
  // userId: number;
  // title: string;
  // body: string;

  constructor(public id = null, public title = null, public userId = null, public  body = null) {
  }
}
